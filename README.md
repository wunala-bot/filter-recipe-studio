# 滤镜照做

一个纯前端的调色配方执行器：选择现成配方，或粘贴 iPhone 相册调色参数，直接在本地预览和保存效果。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

静态产物位于 `dist/`。项目使用相对资源路径，可部署到 GitHub Pages 子路径。

## 隐私

照片只在用户浏览器内处理，不上传到服务器。
